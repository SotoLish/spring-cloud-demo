server:
  port: 8804
spring:
  application:
    name: user-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/springclouddemo?characterEncoding=utf-8&useJDBCCompliantTimezoneShift=true&serverTimezone=UTC
    username: root
    password: root
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8802/eureka
feign:
  hystrix:
    enabled: true

